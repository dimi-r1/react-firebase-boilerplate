export interface ExampleResourceDto {
  // Example response structure - replace with your actual data structure
  id: string;
  name: string;
  description?: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}
